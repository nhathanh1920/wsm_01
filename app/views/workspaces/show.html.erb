<div class="container">
  <div class="row">
    <div class="col-md-12">
      <table id="tableID" border="1">
        <% a=-1%>
        <% for i in 1..@positions.max.column%>
          <tr>
            <% for j in 1..@positions.max.row%>
              <td style= "background: #fff" onclick="myFunction()" width="40px" height="40px">
                <%#= link_to "abc" , "#"" %>
                <%= @positions[a+=1].id %>
              </td>
            <% end %>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
